= simple_form_for menu_element, as: :dynamic_menu_element_types, url: admin_menu_element_path, method: :patch do |f|
  = f.input :title
  = f.input :css
  = f.input :page_id, collection: Page.order(:name), as: :select
  = f.submit class: "btn btn-mini btn-primary"
