= simple_form_for element_type, url: admin_menu_element_type_path, method: :patch do |f|
  = f.input :title
  = f.input :css
  = f.input :page_id, collection: Page.order(:name), as: :select
  = f.submit class: "btn btn-mini btn-primary"
