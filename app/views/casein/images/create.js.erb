<% if image.errors.any? %>
  alert("Failed to upload painting: <%= j image.errors.full_messages.join(', ').html_safe %>");
<% else %>
$('#images').append("<%= j render 'casein/albums/image', object: image %>");
  new Uploader;
<% end %>